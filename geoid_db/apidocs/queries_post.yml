Upload a queries data to the GeoID database.

Input is a JSON array of objects. Both queries objects' id and associated places' query_id is automatically generated by the GeoID database and is not required.
Higher IDs correspond to more recent additions.
---
parameters:
  - name: queries_data
    in: body
    description: Queries data containing array of queries, created by GeoID.
    required: true
    schema:
      $ref: '#/definitions/QueriesData'
responses:
  201:
    description: Number of added queries data.
    schema:
      allOf:
      - $ref: '#/definitions/Content'
      - type: object
        properties:
          results:
            type: object
            properties:
              number_created:
                type: integer
                example: 1
  default:
    description: An error has occured.
    schema:
      $ref: '#/definitions/Error'