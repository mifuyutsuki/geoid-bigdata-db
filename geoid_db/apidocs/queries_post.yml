Upload a queries data to the GeoID database.

Input is a JSON array of objects. ID is automatically generated by the GeoID
database on an incremental basis and is not required. Larger IDs correspond to
newer additions.
---
parameters:
  - name: queries_data
    in: body
    description: Queries data containing array of queries.
    required: true
    schema:
      $ref: '#/definitions/QueriesData'
responses:
  201:
    description: Number of added queries data
    schema:
      type: object
      properties:
        number_created:
          type: integer
          example: 1
  default:
    description: An error has occured
    schema:
      $ref: '#/definitions/Error'